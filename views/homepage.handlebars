{{#each posts as |post| }}
<div class="row align-center mb-5">
    <div class="col-md-6" id="postContainer">
        <a id="postLink" href="/posts/{{post.id}}">{{post.title}}</a>

        <p>Created by: {{post.user.name}}</p>

        <p>{{post.body}}</p>
    </div>
</div>
{{/each}}