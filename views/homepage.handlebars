{{#each posts as |post| }}
<div class="row align-center mb-5">
    <div class="col-md-6" id="postContainer">
        <h2>{{post.title}}</h2>
        
        <p>{{post.body}}</p>
    </div>
</div>
{{/each}}